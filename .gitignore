
# Device-bridge temp files
.fuse_hidden*

# Scratch screenshots and working files from Claude sessions, not part of the app
Claude outputs/

# Supabase CLI scratch
LensTrybe-app/supabase/.temp/

# macOS Finder metadata. Tracked by accident, and it changes every time a folder is
# opened, so it turns up in unrelated commits.
.DS_Store
**/.DS_Store

# Working folder from a session, not part of the app
brag-output/
